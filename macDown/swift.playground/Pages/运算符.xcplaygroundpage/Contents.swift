//: [Previous](@previous)

import UIKit

var str = "Hello, playground"
/*
 比较运算符
 Swift 支持所有 C 的标准比较运算符：
 
 相等 ( a == b )
 不相等 ( a != b )
 大于 ( a > b )
 小于 ( a < b )
 大于等于 ( a >= b )
 小于等于 ( a <= b )
 
 注意
 
 Swift 同时也提供两个等价运算符（ ===  和 !== ），你可以使用它们来判断两个对象的引用是否相同
 
 元组以从左到右的顺序比较大小，一次一个值，直到找到两个不相等的值为止。如果所有的值都是相等的，那么就认为元组本身是相等的。比如说：
 
 
 
 */
var string : String? = "22"
print(string ?? "11")


//区间运算符

1...5 //1-5

1..<5 //1-4

2...
...2


//: [Next](@next)
